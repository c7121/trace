# Remaining architecture questions (resolved)

All decisions and follow-ups from the “remaining questions” walkthrough have been collated into `trace-architecture-summary.md`.

This file is kept as a pointer so links don’t break; it is no longer maintained as a second source of truth (see git history if you want the older Q&A log).
