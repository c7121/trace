# Operators

Operator docs live under `docs/specs/operators/` because operator names and config shapes are part of the DAG surface area (spec), even when implementations are planned.

See: [Operator specs](../../specs/operators/README.md).
